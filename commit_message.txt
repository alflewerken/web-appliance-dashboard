fix(monitoring): Fix metrics history 1H chart spikes and improve data aggregation

MAJOR FIXES:
- Removed duplicate/unused component HostMetricsHistoryTab.js (was not imported anywhere)
- Fixed 1H chart excessive spikes by implementing median aggregation in backend
- Corrected network counter-to-rate conversion for proper B/s display

BACKEND CHANGES (routes/metricsHistory.js):
- Added median aggregation for 1H time range to filter outliers
- Changed 1H from no aggregation to minute-based median aggregation
- Implemented proper median calculation for both traffic and non-traffic metrics
- Fixed aggregation method parameter handling (aggregationMethod vs aggregation)

BACKEND CHANGES (routes/pollingService.js):
- Improved network counter-to-rate conversion with proper delta calculation
- Added robust handling of counter resets and missing data points
- Fixed rate calculation to use actual time differences between samples
- Improved debug logging for network metrics

FRONTEND CLEANUP:
- Deleted unused HostMetricsHistoryTab.js component (458 lines removed)
- MetricsHistory.js is the actual component used in HostPanel.js
- No frontend changes needed as issue was backend aggregation

MONITORING IMPROVEMENTS:
- Added SSEManager service for real-time updates infrastructure
- Created interface_mappings table migration for better network interface tracking
- Enhanced SNMP monitoring with better error handling and metric normalization

RESULT:
- 1H charts now show smooth curves with ~60 data points (1 per minute)
- Median filtering removes outliers and spikes from 10-second polling data
- Network metrics properly show B/s instead of raw counter values
- All time ranges (1H, 6H, 24H, 7D, 30D) now have appropriate aggregation

This fixes the long-standing issue where 1H charts showed excessive spikes
due to displaying all 360 raw data points (10-second intervals) without
proper aggregation. The median filter provides robust outlier removal
while maintaining accurate trend representation.